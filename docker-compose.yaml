# version: '3.8'

services:
  chat-apps:
    build:
      context: .
      dockerfile: ./docker/Dockerfile

    container_name: chat-container

    ports:
      - "3000:80"

    restart: unless-stopped